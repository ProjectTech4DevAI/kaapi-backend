List active model configurations.

Filter by `provider` (`openai` | `google` | `sarvamai` | `elevenlabs` | `anthropic` | `google-vertex` | `proxy` | `google-aistudio`). Paginate with `skip` / `limit` (max 100). Only active models returned, sorted by `provider`, `model_name`. `metadata.has_more` flags more records.
