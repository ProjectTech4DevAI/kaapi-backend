List active projects.

Returns a paginated list of active projects across all organizations. The response includes a `has_more` field in `metadata` indicating whether additional pages are available.

Pass the optional `search` query parameter to filter projects by name (case-insensitive substring match).
