List all users that belong to the current project.

Returns user details including their active status — users added via invitation will have `is_active: false` until they complete their first Google login.
