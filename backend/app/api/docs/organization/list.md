List active organizations.

Returns a paginated list of active organizations in the system. The response includes a `has_more` field in `metadata` indicating whether additional pages are available.

Pass the optional `search` query parameter to filter organizations by name (case-insensitive substring match).
