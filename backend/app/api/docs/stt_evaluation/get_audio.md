Retrieve metadata for a specific audio file by its ID.

Returns detailed information about an uploaded audio file, including its S3 location, size, content type, and timestamps. You get a pre-signed S3 URL for direct file access as well if you have set ``include_url=True``.
